# Microscope_Environment
Simulates a microscope which captures images from cells and an LLM Agent optimizes parameters of the microscope to enhance the image quality based on metrics. Can be used a standalone MCP Server and connected to any local LLM.
